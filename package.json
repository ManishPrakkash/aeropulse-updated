{
  "name": "aeropulse",
  "version": "1.0.0",
  "description": "Aeropulse Respiratory Monitoring System",
  "scripts": {
    "build": "cd client && npm install --force && npm run build",
    "start": "cd client && npm run dev"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "html2canvas": "^1.4.1",
    "jspdf": "^3.0.1",
    "jspdf-autotable": "^5.0.2"
  },
  "private": true
}
